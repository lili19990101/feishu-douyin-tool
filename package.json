{
  "name": "feishu-douyin-tool",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install && cd ../backend && npm install",
    "build": "npm --prefix ./frontend run build && node ./scripts/postbuild.js",
    "start": "npm --prefix ./backend run start"
  },
  "dependencies": {},
  "devDependencies": {}
}
